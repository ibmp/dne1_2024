OUTDIR = Path(config["outdir"])

RAWDATA_DIR = Path(config["rawdata_dir"])
RUNS_DIR = Path(config["runs_dir"])
MAPPING_DIR = Path(config["mapping_dir"])
GENOME_DIR = Path(config["genome_dir"])
RESULTS_DIR = Path(config["results_dir"])

GENOME = Path(config["reference"]["genome"])
TRANSCRIPTOME = Path(config["reference"]["transcriptome"])
ANNOTATION = Path(config["reference"]["annotation"])